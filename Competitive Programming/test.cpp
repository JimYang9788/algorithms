#include <bits/stdc++.h>

using namespace std;

int main(){
    int a, b;
    string x;
    cin >> a >> b >> x >> "flush action";

    cout << a << " " << b << " " << x << "\n"<<flush;
    return 0;
} 